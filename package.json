{
  "name": "b1gmail",
  "version": "0.0.1",
  "private": true,
  "license": "GPL-2.0",
  "scripts": {
    "lint": "composer run-script lint && npm run lint-additional-files",
    "lint-additional-files": "prettier --check **/*.{css,md,json,yml}"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.2",
    "prettier": "^2.8.2"
  }
}
