{
  "name": "b1gmail",
  "version": "0.0.1",
  "private": true,
  "license": "GPL-2.0",
  "scripts": {
    "lint": "composer run-script lint && lint-additional-files",
    "lint-additional-files": "prettier --check **/*.{md,json,yml}"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.18.9",
    "prettier": "^2.7.1"
  }
}
